before_action :authenticate_user? 

<% @todos.each_slice(3) do |row| %>
<% row.each do |todo| %>
<div class= "col">
 <div class="card" style="width: 10rem;">
  <%= image_tag todo.photo %>
    <div class="card-body">
    <h5 class="card-title"><%= todo.name  %></h5>
    <p class="card-text"><%= todo.description  %></p>
    </div>
        <li class="list-group-item">completado o no completado</li>
      <div class="card-body">
        <a href="#" class="card-link">Completado</a>
      </div>
 </div>
</div>